name: Docker Image CI


on:
  push:
    branches:
      - '**'

jobs:

  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Run tests
      run: "pwd; ls; ./test/suite/test.sh \"$PWD\""
